import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'resume.ranker.app',
  appName: 'Automated Hr Resume Ranker',
  webDir: 'dist'
};

export default config;
